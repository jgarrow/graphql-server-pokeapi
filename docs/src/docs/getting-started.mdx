---
title: 'Getting started'
description: Learn how to get started with your own GraphQL Pokédex server.
---

import { Link } from 'gatsby';
import pokeapi_graphql_docs_gif from '../assets/pokeapi_graphql_docs_gif.gif';
import pokeapi_graphql_schema_gif from '../assets/pokeapi_graphql_schema_gif.gif';

## Setting up your own server

Let's start by cloning the GitHub repo and downloading it to your local machine.

1. Navigate to [https://github.com/jgarrow/graphql-server-pokeapi](https://github.com/jgarrow/graphql-server-pokeapi). Click on the green `Code` button in the upper righthand corner and copy the HTTPS url to your clipboard.

2. Open your terminal and download the repo to your computer, pasting in that url from GitHub after `clone`:

```bash
git clone https://github.com/jgarrow/graphql-server-pokeapi
cd graphql-server-pokeapi
```

3. You have a couple of options here. The repo from GitHub is a monorepo that contains both files for this documentation site (`/docs`) and the files for the actual server (`/server`).

-   If you don't want the documentation site files, you can delete the entire `/docs` folder located at the root of the repo. Move the contents of `/server` out into the root and delete the `/server` folder so that everything is at the root level. Then you can continue on to step 3.
-   If you want to keep the documentation site, navigate to the `/server` directory before moving on to step 3.

```bash
# if you're keeping the documentation site, navigate to the `/server` directory before install the dependencies
cd server
```

3. Install the dependencies:

```bash
npm install
```

4. Spin up your development server!

```bash
npm run start
```

\*\*`nodemon` has been installed for hot reloading whenever changes are made and saved. This way, you don't have to kill and restart the server every time you save changes. To use this feature, use the following start command instead:

```bash
npm run server
```

5. Navigate to [http://localhost:4000/](http://localhost:4000/) and start querying!

## Schema and docs

You can find documentation about the schema and query fields at <Link to="/queries/base-queries">Base queries</Link> and <Link to="/queries/nested-query-types">Nested query types</Link>.

You can also check out the "Schema" and "Docs" tabs in the <Link to="/playground">GraphQL playground</Link>

### GraphQL playground schema tab

<img
    src={pokeapi_graphql_schema_gif}
    alt="Gif showing the schema tab in the GraphQL Playground"
/>

### GraphQL playground docs tab

<img
    src={pokeapi_graphql_docs_gif}
    alt="Gif showing the docs tab in the GraphQL Playground"
/>
